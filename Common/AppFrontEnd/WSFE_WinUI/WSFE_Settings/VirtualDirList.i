%

interface virtualDirList supports dialog
    open core

predicates
    setVirtualDirList : (namedValue_list PerformResultParams).

predicates
    existName : (string Name) determ.

end interface virtualDirList