%

interface localOptions supports dialog
    open core

predicates
    initLocalOptions : (string LocalOptions,xmlDocument XmlOptions).

predicates
    addControlsListners : (commandTab CommandTab).

end interface localOptions